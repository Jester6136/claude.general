# Roadmap & TODO — <PROJ>

> **MỘT** nơi ghi mọi vòng "raise issue → fix → verify" (nếu dùng O7-B). Không tạo `ISSUES.md`/
> `CHANGELOG.md` rời — phân mảnh tri thức là cách mất nó. Nếu project đã chuyển sang O7-A (GitHub/GitLab
> Issue là nguồn việc-đang-mở), phần §TODO dưới đây chỉ còn giữ LỊCH SỬ, không còn nhận mục mới.

## 1. Năng lực đã dựng

<Bảng/liệt kê những gì đã chạy được, theo primitive/module — không phải nhật ký, mà là TRẠNG THÁI hiện tại.>

## 2. Lộ trình

<Các pha, phụ thuộc giảm dần / rủi ro tăng dần — xem CLAUDE.md §2 cho cách chọn quy mô quy trình.>

## 3. ★ TODO — việc còn mở (ưu tiên trên xuống)

Mẫu mỗi mục (xem `CLAUDE.md` §5.4):
```markdown
- [ ] **<mức khẩn 🔴/🟡/🟢> — <tên ngắn>** — ⬜ chưa làm (<repo>, `<file>` nếu cần): <gốc rễ, nếu đã biết>.
- [x] **<mức khẩn> — <tên ngắn>** — ✅ **code+verify <YYYY-MM-DD>** (<repo>, `<file>`): <gốc rễ + fix, 1-2 câu>.
      **Verify:** <lệnh thật → kết quả thật — PHẢI chạy lại trong phiên hiện tại trước khi ghi, xem
      AGENT.md §6>. ⚠**Còn:** <phần dở, nếu có>.
      ★<bài học dễ lặp ở chỗ khác>
```

<Bắt đầu ghi TODO thật ở đây.>

## Đọc tiếp
- [HANDOFF.md](HANDOFF.md) — trạng thái phiên gần nhất.
